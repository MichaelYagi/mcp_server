{
  "name": "summarize_direct",
  "description": "Prepare text for direct summarization in a single LLM call.",
  "input_schema": {
    "type": "object",
    "properties": {
      "text": {
        "type": "string",
        "description": "The full text to summarize."
      },
      "style": {
        "type": "string",
        "description": "Summary style: short, medium, or long.",
        "enum": ["short", "medium", "long"],
        "default": "medium"
      }
    },
    "required": ["text"]
  },
  "output_schema": {
    "type": "string",
    "description": "JSON string containing the text and style for direct summarization."
  }
}
