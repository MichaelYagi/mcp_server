{
  "name": "get_location",
  "description": "Resolve a location using auto‑detected defaults or an optional city and country.",
  "input_schema": {
    "type": "object",
    "properties": {
      "city": {
        "type": "string",
        "description": "City name (e.g., Tokyo, Toronto, London). Optional."
      },
      "state": {
        "type": "string",
        "description": "State, province, prefecture name (e.g., Kanagawa, BC, Washington). Optional."
      },
      "country": {
        "type": "string",
        "description": "Country name (e.g., Japan, Canada, UK). Optional."
      }
    },
    "required": []
  }
}
