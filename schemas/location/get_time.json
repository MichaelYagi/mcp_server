{
  "name": "get_time",
  "description": "Get the local time for a resolved location.",
  "input_schema": {
    "type": "object",
    "properties": {
      "city": { "type": "string" },
      "country": { "type": "string" },
      "timezone": {
        "type": "string",
        "description": "Optional explicit timezone (e.g., Asia/Tokyo)"
      }
    },
    "required": []
  }
}
