{
  "name": "vector_search",
  "description": "Search the vector database for similar embeddings.",
  "input_schema": {
    "type": "object",
    "properties": {
      "query": { "type": "string" },
      "top_k": { "type": "number", "default": 5 }
    },
    "required": ["query"]
  }
}
